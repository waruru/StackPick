<div id="<%= dom_id tag %>" class="bg-blue-200 inline-flex items-center text-sm rounded-2xl overflow-hidden">
  <span class="mx-2 leading-relaxed truncate max-w-xs px-1" x-text="tag"><%= tag.name %></span>
</div>
