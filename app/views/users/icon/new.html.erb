<%= turbo_frame_tag "modal" do %>
  <div class="p-10" data-controller="turbo-modal">
    <h1 class="font-bold text-4xl">プロフィールアイコンアップロード</h1>
    <%= render 'form', user: @user %>
    <%= button_tag "キャンセル", data: { action: "turbo-modal#hideModal" }, type: "button" %>
  </div>
<% end %>
